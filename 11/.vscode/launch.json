{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "运行 main (编译 main+Lambda)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/main.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "environment": [],
      "console": "integratedTerminal",
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "preLaunchTask": "build main (main + Lambda)"
    }
  ]
}
